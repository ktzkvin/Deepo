x-base-image: &base-image
  build: .
  image: deepo-base:latest

services:
  deepo:
    <<: *base-image
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload